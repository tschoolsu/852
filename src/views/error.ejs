<%- include('partials/head', { title, bodyClass: 'auth-page' }) %>
<%- include('partials/nav') %>
<main class="single-card-shell">
  <section class="auth-panel narrow error-panel">
    <span class="error-code">!</span>
    <h1><%= title %></h1>
    <p><%= message %></p>
    <a class="primary-button inline" href="<%= user ? '/app' : '/login' %>">返回<%= user ? '檔案庫' : '登入' %></a>
  </section>
</main>
<%- include('partials/footer') %>
